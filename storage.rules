rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // الكتاب خاص — ما أحد يوصله إلا عبر رابط موقّع يطلعه السيرفر بعد الدفع
    match /books/{file=**} {
      allow read, write: if false;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
